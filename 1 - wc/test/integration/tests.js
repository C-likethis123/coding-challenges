describe("Standard Input", () => {
    // TODO: how to test that 'wc' alone waits for standard input
})

describe("Options specified", () => {
    test('individual flags works', () => {
        // start the app
        // test '-c'
        // test '-l'
        // test '-w'
    });
    test('mulitple flags works', () => {

    });
    test('single flags works', () => {

    });
})